Changelog
=========

This repository does not currently maintain a curated changelog file.

For now:

- Use the Git history as the source of truth.
- Pin forge bundles by immutable URLs (commit SHA) when publishing demos.

